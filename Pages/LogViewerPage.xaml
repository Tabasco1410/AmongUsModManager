<Page
    x:Class="AmongUsModManager.Pages.LogViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ログビューア" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" Margin="0,0,0,12">
            <Button Content="更新" Click="Refresh_Click" Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="ログフォルダを開く" Click="OpenFolder_Click"/>
            <Button Content="クリア" Click="Clear_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" x:Name="LogScrollViewer"
                      Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                      CornerRadius="8" Padding="12">
            <TextBlock x:Name="LogText"
                       FontFamily="Consolas" FontSize="12"
                       TextWrapping="Wrap" IsTextSelectionEnabled="True"/>
        </ScrollViewer>
    </Grid>
</Page>
