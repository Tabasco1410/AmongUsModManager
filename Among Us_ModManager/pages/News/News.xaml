<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Among_Us_ModManager.Pages.News"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock FontSize="28"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,10"
                   Text="お知らせ"/>

        <!-- メインコンテンツ -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ニュース一覧 -->
            <ListBox BorderBrush="Gray"
                     BorderThickness="1"
                     Grid.Column="0"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     SelectionChanged="NewsListBox_SelectionChanged"
                     x:Name="NewsListBox">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Background="LightGray"
                                   Cursor="Hand"
                                   FontSize="14"
                                   Margin="5,2"
                                   Padding="5"
                                   Text="{Binding Title}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- 詳細表示 -->
            <Border BorderBrush="Gray"
                    BorderThickness="1"
                    CornerRadius="5"
                    Grid.Column="1"
                    Margin="10,0,0,0"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Date -->
                        <RowDefinition Height="Auto"/>
                        <!-- Title -->
                        <RowDefinition Height="*"/>
                        <!-- WebBrowser -->
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               FontSize="14"
                               FontWeight="Bold"
                               Margin="0,0,0,5"
                               x:Name="DetailDateTextBlock"/>

                    <TextBlock Grid.Row="1"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,10"
                               TextWrapping="Wrap"
                               x:Name="DetailTitleTextBlock"/>

                    <!-- WebBrowserが下に広がるようにする -->
                    <WebBrowser Grid.Row="2"
                                Margin="0"
                                x:Name="DetailWebBrowser"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 戻るボタン -->
        <Button Click="BackButton_Click"
                Content="戻る"
                Grid.Row="2"
                Height="40"
                HorizontalAlignment="Left"
                Margin="0,10,0,0"
                Width="100"/>
    </Grid>
</Page>
