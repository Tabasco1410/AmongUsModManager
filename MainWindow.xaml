<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AmongUsModManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AmongUsModManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AmongUsModManager">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,8,0,8" Grid.Row="0">
            <Image Source="/Resource/Icon.png" Width="16" Height="16" Margin="0,0,12,0"/>
            <TextBlock Text="AmongUsModManager" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <NavigationView x:Name="NavView" 
                        Grid.Row="1"
                        PaneDisplayMode="Left" 
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"                       
                        SelectionChanged="NavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="HomeItem" Content="ホーム" Tag="Home">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="ModInstallItem" Content="Modインストール" Tag="ModInstall">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7BF;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator x:Name="NavSeparator"/>

                <NavigationViewItem x:Name="LibraryItem" Content="ライブラリ" Tag="Library">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B1;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>              
                
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="InfoItem" Content="このアプリについて" Tag="Information">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Grid.Row="1"/>

        </NavigationView>
    </Grid>
</Window>
